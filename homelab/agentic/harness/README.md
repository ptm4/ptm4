# Harness notes

Notes on the Claude Code harness itself — permission allowlists, hooks, settings — as
opposed to `runbooks/` (how the homelab runs) or `skills/`/`rules/` (agent behavior).

Nothing here yet. See `.claude/settings.local.json` and
`.claude/ONBOARDING.md` §2 for the current state until this gets written up properly.
