---
name: add-to-rpi-webapp
description: Add a generated static page (dashboard, report, diagram, tool) to the homelab dashboard at webapp.rpi.lan. Use when the user asks to add/publish a page to the homelab webapp.
---

# add-to-rpi-webapp

> ⚠️ RECOVERY STUB — the original body of this skill was lost when the noblenumbat
> `~/code/ptm4` copy was deleted (2026-07-22). It was gitignored, so it is not in git
> history, and no surviving Claude transcript captured its full contents — only the
> `description` above (from a skill listing) could be recovered.
>
> Please re-author the procedure. It published a generated static page to the homelab
> dashboard served at `webapp.rpi.lan` (rpi). See [[project_discord_weather_bot]]-style
> webapp management and the RPI-srv webapp under `homelab/RPI-srv/webapp/`.
