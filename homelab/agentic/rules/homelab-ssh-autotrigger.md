# homelab SSH auto-trigger rule

> ⚠️ RECOVERY STUB — original content lost with the noblenumbat copy (2026-07-22),
> gitignored and not captured in any surviving transcript (it only appeared as a
> planned file, never written). Re-author from intent below.

Intent (from transcript context): a standing rule that auto-triggers the
[`homelab-ssh`](../skills/homelab-ssh/SKILL.md) skill whenever a query names a homelab
host (opti / rpi / noblenumbat / android) and acting on it requires actually logging in.
