{
  "name": "webapp-backend",
  "version": "3.0.0",
  "private": true,
  "description": "Homelab dashboard backend — v3.Fable: the v2 Fastify app plus SSE, incidents and hosts routes",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "@fastify/multipart": "^9.0.3",
    "@fastify/static": "^8.2.0",
    "fastify": "^5.11.3",
    "fastify-plugin": "^5.0.1"
  }
}
