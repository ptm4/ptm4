# engine/

Pure C# (netstandard2.1) class library + xUnit tests. No `UnityEngine` references, ever.
Holds the 5e rules engine, storybook runtime and monster AI. Scaffolded by Plan 01; designed
by Plan 03. Run `dotnet test` here to validate rules without opening the Editor.
