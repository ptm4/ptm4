# assets-src/

Pre-import art with a manifest (file, sha256, source prompt/tool, date). Planned subfolders:
`sprites/`, `voxels/`, `palettes/`, `portraits/`, `ui/`. Any `batches/` or `inbox/` folder
is gitignored; only curated, cleaned outputs are tracked. Pipelines: Plan 05 (sprites) and
Plan 06 (voxels).
