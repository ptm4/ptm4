# Decisions log

Append-only. One row per decision. Supersede by adding a new row that references the old
one, never by editing history. Dates are absolute.

| # | Date | Area | Decision |
|---|---|---|---|
| D1 | 2026-09-09 | Presentation | 2.5D isometric, realised as **HD-2D**: pixel-art billboard sprites in a 3D-lit voxel world. |
| D2 | 2026-09-09 | Spine | **CRPG-style** (engine runs NPCs/encounters/dialogue), self-sufficient at runtime. |
| D3 | 2026-09-09 | Token policy | LLM tokens spent at **authoring time only** (Claude Max). Runtime = data files + local engine. Optional local-LLM improv (host GPU, llama.cpp/LLamaSharp, Vulkan backend, CPU fallback, small GGUF). Optional Claude-backed DM switch, never required. |
| D4 | 2026-09-09 | Table | 1 human DM + 3-5 remote players; DM not always present, so auto-DM must run a session. |
| D5 | 2026-09-09 | Content | Peter pre-authors whole campaigns (original, or adaptations for private use). Only SRD ships in builds; adapted copyrighted modules stay in gitignored `content/private/`. |
| D6 | 2026-09-09 | Art refs | Octopath Traveler + Rain World + Sea of Stars; Cloudpunk lighting/mood as stretch. Peter to supply a reference image for the style bible (Plan 04). |
| D7 | 2026-09-09 | World kit | Voxel modular kit (MagicaVoxel to Unity), grid-snapped; procedural voxel gen from palettes optional. |
| D8 | 2026-09-09 | Sprites | Sea of Stars scale (~64-96 px tall); generate large, downscale + palette-quantize via cleanup script. Portrait + billboard; layered paper-doll (body/armor/weapon). Monsters as sprites (assumed, not tokens). |
| D9 | 2026-09-09 | Animation v1 | idle/walk/attack/hit/death, 4 facings, ~20-30 frames per creature from one sheet template. |
| D10 | 2026-09-09 | Time model | **100% turn-based**, exploration included. |
| D11 | 2026-09-09 | Roleplay | Authored dialogue trees + skill checks; voice in Discord with engine enforcing rules; local-LLM improv; optional Claude switch. |
| D12 | 2026-09-09 | Auto-DM scope | Run storybook; tactical monster AI; full rules adjudication; procedural side content. |
| D13 | 2026-09-09 | Player view | Own PC, own camera, shared world, per-player fog of war; DM sees/moves all. |
| D14 | 2026-09-09 | Edition | **2024 rules (SRD 5.2) first**, 2014 (SRD 5.1) layered in where 2024 data/spec can't be met. Every rules entry tagged with ruleset. |
| D15 | 2026-09-09 | Sources | SRD 5.1 + 5.2 (CC-BY-4.0), Open5e API + 5e-database (SRD-derived), homebrew format. **Excluded:** 5e.tools or any non-SRD data in repo/builds. |
| D16 | 2026-09-09 | Networking | Netcode for GameObjects over Unity Relay + Lobby, join codes, host-authoritative. |
| D17 | 2026-09-09 | Delivery | Windows x64 + macOS + Linux standalone. Download hosting (opti web vs itch.io private) **undecided**. |
| D18 | 2026-09-09 | Image gen | Codex CLI / ChatGPT app agent-driven; Claude writes prompt packs, sheet templates, cleanup pipeline. (Codex CLI not on PATH on 2026-09-09; open item.) |
| D19 | 2026-09-09 | Sequence | POC slice, then headless rules engine, then local beta, then friends test over Relay, then complete & ship. |
| D20 | 2026-09-09 | Tooling | Unity 6000.6.0f1 (Hub), URP, C#; dotnet 8 SDK present for the headless engine; official Unity plugin for Claude Code + Unity MCP bridge (`com.unity.ai.assistant`). |
| D21 | 2026-09-09 | Layout | Plans + engine + content + tools live in `homelab/DND.vbeta` (this repo). Unity project lives at `E:\Unity\Projects\DND` in its own git repo and consumes `engine/` as a local UPM package. |
| D22 | 2026-09-09 | Editor control | Live Editor control goes through the **Unity CLI + `com.unity.pipeline`** route (`unity mcp configure claude`), which is what the installed plugin's skills assume. The `com.unity.ai.assistant` MCP bridge is the documented fallback only; never run both (Modding.md rule 8). |
| D23 | 2026-09-09 | Layout | Supersedes the path in D21: Peter created the Unity project as **`E:\Unity\Projects\Dungine`** (URP-blank template 17.x, Unity 6000.6.0f1, git initialized, Unity Cloud project linked). All plans use that path. `com.unity.pipeline` 0.6.0-exp.1 and `com.unity.ai.assistant` 2.19.0-pre.2 are both installed by the template; only the Pipeline route is registered with Claude Code (`unity-editor-mcp`, user scope), per D22. |
| D24 | 2026-09-09 | Delivery | Resolves the open half of D17: **no hosting infrastructure in v1.** Peter hands friends the build (exe/zip) himself. Plan 14 keeps builds + versioning + install notes; drops the download page and update check. |
| D25 | 2026-09-09 | Image gen | Refines D18: **Peter runs the Codex/ChatGPT image agent himself.** Claude's job is to produce hand-off plans (Plan 05/06 prompt packs, templates, inbox layout) that Peter gives to Codex. Claude never invokes Codex. |
| D26 | 2026-09-09 | Engine wiring | `engine/DND.Engine/` is the UPM package root (package.json + asmdef + Runtime/*.cs) referenced from Dungine's manifest as a `file:` local package, not a junction. The same sources build headless via `DND.Engine.csproj`; bin/obj are redirected to `engine/.build/` so Unity never imports DLLs. C# 9 / netstandard2.1 / no UnityEngine. |
| D27 | 2026-09-09 | Art numbers | From `docs/style-bible.md` v0.1: palette **Dungine-54** (`tools/palette.py` is the source; outline ink `#0b0a12`); **PPU 64**; Medium/Small cell **64x96** (body box 40x72, feet 4 px above bottom), Large 128x128; **3 generated facings (S, N, E), W mirrored**; 20 frames per facing (idle 4, walk 6, attack 4, hit 2, death 4); Medium sheet 1280x288; voxels **16 per tile**, walls 32; camera perspective FOV 28° pitch 38°; palette *modes* are lighting profiles (Day / Night), not separate palettes. Numbers marked "open for review" in the bible may move after the POC. |
| D28 | 2026-09-09 | Camera | POC finding supersedes the 38° in D27: **pitch 50°, distance ~11 u, FOV 28°**, and the game must cut away / dither walls between camera and party. Lighting baseline for night scenes: ambient `night_purple_3`, moon 1.6, torch point lights 40 / range 9, post exposure +0.6, DoF band starting beyond the subject distance. |
| D29 | 2026-09-09 | Sprite pipeline | Codex's image tool cannot emit alpha and outputs a fixed ~1774x887. The pipeline keys painted checkerboard/white backgrounds and accepts any slot size (`sprite_clean` 0.2.0); prompts ask for a flat white background; a turnaround alone builds a placeholder sheet. First real asset: `goblin` (turnaround, approved by the pipeline, awaiting Peter's eye). |
| D30 | 2026-09-09 | Palette | Peter approved (via Codex) the leather-armored, olive-skinned goblin and the **`goblin_olive` ramp appended to the master palette (Dungine-57)**. Appending keeps voxel indices stable. Goblin identity in PROMPT-PACK §3 is superseded by `inbox/goblin/turnaround.png`. Codex also edited `docs/style-bible.md` §2 for this; accepted. |
| D31 | 2026-09-09 | Sprite pipeline | **Scale policy:** one scale factor per facing (from that facing's idle_0); frames are never fitted individually. Animations whose mean height drifts more than 12 % from idle fail the build with a "regenerate strip X" message (death exempt). Weapon extension must stay inside the 64 px cell; clipped sides are reported. Codex does not edit the scripts; it regenerates what the tool names. |
| D32 | 2026-09-10 | Sprite pipeline | Refines D31: the per-facing scale targets a **stature height**, not the body box (`sprite_clean` 0.4.0, `--stature T/S/M/L` = 36/52/72/104 px). Box-filling made the goblin 62 px and the fighter 92 px depending on pose width. Goblin and giant rat are Small (52); the other Medium creatures 72; giant spider and ogre Large (104, `--size L`). Manifest records stature. |
| D33 | 2026-09-10 | Sprite pipeline | Codex's `generate-unity-creature-sprites` skill (written by Codex from the first four runs) is the canonical Codex procedure; a tracked copy lives at `ptm4/.agents/skills/generate-unity-creature-sprites/` (Codex's user copy in `~/.codex/skills/` is the working one; keep them in sync). Bulk production is delegated: turnaround approval is pre-granted for any turnaround matching its identity block (`BATCH-RUN.md`); Claude verifies sheets after each batch. Second roster of 10 monsters added (PROMPT-PACK §3b). |
| D34 | 2026-09-10 | Sprite pipeline | `sprite_clean` 0.5.0: background keying also removes **enclosed islands** (components not reached from the border whose pixels match a background color within 10/channel and span >= 24 source px). Found by Codex on ghoul/zombie/bandit; skeleton is the regression fixture (bone survives). Rule of engagement confirmed: Codex reports script defects with evidence and stops; the owner fixes, rebuilds, and tells it where to resume. |
| D35 | 2026-09-10 | Sprite pipeline | **Quadruped contract:** a Medium quadruped is one tile long, so quadrupeds/long creatures build with `--fit width` (`sprite_clean` 0.6.0): each facing's reference pose fills the cell width unless that exceeds the stature height. Bipeds keep `--fit height`. Turnaround-only builds now scale per facing. Wolf: S 72x35, N 73x34, E 36x63. |
| D38 | 2026-09-11 | Sprite pipeline | `sprite_clean` 0.7.0: only **light** border colors (mean channel >= 180) may be treated as background. Found by Astra on the ogre portrait (a bust touching the border made its brown a "background" and erased the face); regression: that source now keys only white. All 24 sheets re-checked PASS. Astra's white-gutter portrait convention stays in the prompt pack as belt-and-braces. |
| D39 | 2026-09-11 | Voxel kit | **Codex writes the `.vox` files programmatically** (its image tool cannot make voxels): `tools/vox_write.py` (numpy → VOX 150 with the master palette in the RGBA chunk) and `tools/vox_to_obj.py` (voxel → OBJ, 1/16 unit, Y-up, floor-center origin, colors via a 16x16 palette atlas, emissive faces in a second material). Fable owns `tools/vox_check.py` (contract validator, written and self-tested) and the Unity importer. Dungeon biome tile list v1 fixed in Plan 06; `kit.json` schema is the metadata contract Plans 08/12 consume. |
| D40 | 2026-09-11 | Characters | Peter's play review: sprites read as "2D cutouts". Decision: **keep pixel sprites (D1/D8 stand) and ground them**: contact-shadow blob, cast shadows, cool fill light, lean ≤ 5°. Not extruded voxels, not 3D models. Applies to Plans 04 §5 and 11. |
| D41 | 2026-09-11 | Voxel kit | Import conventions fixed by the first kit: `height_tiles` is a **float** (props are fractional); OBJ pivot is the footprint floor center, so **floors are placed at y = -3/16** (their top is the walking plane) and everything else at y = 0; the sconce's flame is on the tile's **-Z face**, rotated toward the floor neighbour with yaw = atan2(-dir.x, -dir.y); 2x1 props are centered between their two cells; kit `blocks_move` metadata feeds GridMap. The Unity project holds only imported copies; the repo kit is the source. |
| D42 | 2026-09-11 | Process | **Agent bridge** (Astra's proposal, Fable's implementation): `homelab/DND.vbeta/tools/bridge/` dispatcher with SQLite queue, outbox envelopes, context packets, one run per agent, Editor exclusivity by scheduling (advisory beyond that), post-run scope audit, 2 review cycles then `decision_required`, usage-limit auto-pause, no commits/models/billing changes. Runbook `homelab/agentic/runbooks/11-dungine-agent-collaboration.md`. Acceptance tests 16/16. **Disabled until Peter sets `enabled: true`.** Deviations from the proposal are listed at the top of `Plans/AGENT-COLLABORATION-PROPOSAL.md`. |
| D43 | 2026-09-12 | Engine | **Objects are engine state** (Sonnet batch 01 S3): `WorldObject`/`ObjectTemplate` with DMG AC-by-material and HP-by-size, `InteractIntent` (free interaction once per turn, then the Utilize action; Athletics to force, Sleight of Hand + thieves' tools to pick) and `AttackObjectIntent` (poison/psychic immune; broken objects stop blocking). Layering kept: the `Interaction` enum lives in `Model`, so `Events` never depends on `Intents`. |
| D44 | 2026-09-12 | Engine | **The POC is not wired to the engine.** Plan 02 stays throwaway; its door/turn logic is Unity-side and will be deleted. The first engine-driven Unity scene is a new milestone ("encounter shell", Plan 11 first half): `Encounter` + `content/rules` loader on the host, presentation only in Unity. No more rules logic goes into `Assets/Dungine/POC/`. |
| D45 | 2026-09-12 | Data | **Rules data envelope** (Plan 03a): every record is `{id, kind, ruleset, name, source, source_url, license, attribution, data, raw}`; `data` is the normalized view the engine loads, `raw` the untouched source record. 2024 comes from 5e-database except spells (Open5e v2 `srd-2024`); 2014 from 5e-database only. Attribution strings use the version the source states ("SRD 5.2", "SRD 5.1"). The Open5e vocabulary is normalized to 5e-database's (ability abbreviations, class slugs). |
| D46 | 2026-09-12 | Tooling | **No pip dependencies, but the system curl is allowed as a transport fallback**: when Python's OpenSSL rejects a host's certificate chain that the OS trust store accepts (Open5e, 2026-09-11), `ingest_srd.py` retries through `curl`, which still verifies. Certificate verification is never disabled anywhere in the tools. |
| D47 | 2026-09-12 | Engine | **JSON loader design** (batch 02): the engine gets its own minimal JSON reader (`DND.Engine.Data.Json`, ~200 lines, no System.Text.Json, no UnityEngine) that produces plain `Dictionary<string,object>`/`List<object>`/`string`/`double`/`bool`/`null`, and a `Compendium` that maps `content/rules` envelopes to templates by `ruleset` with 2024→2014 fallback per id. Data files are loaded by the host from disk (Unity: `StreamingAssets/rules/`), never embedded in code. |
| D48 | 2026-09-12 | Content | **First module: *Baldur's Gate: Descent into Avernus* (WotC 2019), adapted for Peter's home table.** Per D5/D15: the book's text, maps, art and non-SRD stat blocks never enter the repo or a build; Peter authors the adaptation in the Plan 07 storybook format under gitignored `content/private/avernus/`, loaded at runtime from a local folder so builds stay clean. Everything we make ourselves (voxel biomes, sprites, portraits, UI) is original work and ships normally, Avernus-themed or not. Build order: chapter 1 first (Baldur's Gate streets, Elfsong Tavern, Dungeon of the Dead Three) because it is a low-level urban dungeon crawl the existing dungeon kit already serves; the infernal biome and war machines come after. Plan 07's schema is designed against chapter 1 as its driving example; Plan 13 is this adaptation. |
| D49 | 2026-09-12 | Direction | **The POC becomes the app** (Peter): supersedes D44's "new scene". `POC.unity`, the kit, camera, HUD, tiles and DM setup stay and grow into Dungine; the rules underneath are swapped for the engine piece by piece (TurnManager → presenter over `Encounter`, units from `Compendium`, DM setup gains prop placement). Phases: A Dungine core → B module & campaign → C assets at scale for DiA → D beta & ship; see `ROADMAP.md`. Rules logic still never lives in Unity code (architecture invariant kept). |
| D36 | 2026-09-10 | Process | While Fable is unavailable, **Astra (Codex) holds delegated authority** for the sprite batch: small evidence-backed script fixes allowed with version bump + D-row + regression note + full re-check; palette, cell geometry, templates, tolerances are off limits; no Unity installs. Recorded in `ptm4/AgentComms.md`. |
| D37 | 2026-09-10 | Sprite pipeline | Astra under D36 applies D35's existing `--fit width` to **giant_spider**, retaining `--size L --stature L`: the wide flat eight-leg silhouette at height fit measured S/N/E 276/284/280 px in 128 px cells and lost ground contacts after clipping. Width fit builds OK and checks PASS (only N 1 px overhang). No script, palette, cell, template, inset or tolerance change. Ogre/owlbear remain height-fit bipeds. |
